export const calculateSalary = (basic: number, gender: string) => {
  let salary;
  // salary* multiplier
  switch (gender) {
    case "male":
        
      break;
    case "female":
      break;

    default:
      break;
  }
};
